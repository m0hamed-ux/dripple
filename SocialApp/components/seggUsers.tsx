import { View, Text } from "react-native";

export default function SeggUsers() {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>segg People</Text>
        </View>
    );
}